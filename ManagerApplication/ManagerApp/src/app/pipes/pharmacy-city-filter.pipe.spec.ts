import { PharmacyCityFilterPipe } from './pharmacy-city-filter.pipe';

describe('PharmacyCityFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PharmacyCityFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
